{
  "name": "capstone-project",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm i forever --prefix ./frontend && set NODE_ENV=test-backend && jest --verbose",
    "start": "node backend/index.js",
    "delete-db": "del-cli db.sqlite",
    "migrate": "npm run delete-db && knex migrate:latest --knexfile knexfile.js",
    "seed": "knex seed:run --knexfile knexfile.js",
    "db": "npm run migrate && npm run seed"
  },
  "author": "keetmalin.13@cse.mrt.ac.lk",
  "license": "ISC",
  "jest": {
    "testEnvironment": "node",
    "preset": "jest.config.js",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.17.3",
    "knex": "^2.1.0",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.22.5",
    "babel-jest": "^29.5.0",
    "cross-env": "^7.0.3",
    "del-cli": "^4.0.1",
    "jest": "^27.5.1",
    "supertest": "^6.3.3"
  }
}
